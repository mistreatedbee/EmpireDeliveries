// Legacy screen — superseded by app/(auth)/login.tsx and app/(auth)/register.tsx
import React from 'react';
import { View, Text } from 'react-native';

export default function AuthScreen() {
  return <View />;
}
